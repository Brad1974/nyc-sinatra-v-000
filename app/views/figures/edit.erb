<h1> Edit Your Figure </h1>

<form method="POST" action="/figures/<%=@figure.id%>">
<input type="hidden" name="_method" value="patch">
<h2>What is the figure's new name?</h2>
<p><input type="text" id = "figure_name" name="figure[name]" value="<%=@figure.name%>">Figure Name</p>



<h2>Choose the Figure's Landmark(s)</h2>
<% Landmark.all.each do |landmark| %>
  <label><%= landmark.name %></label>
  <input type="checkbox" id="landmark_<%=landmark.id%>" name="figure[landmark_ids][]" value="<%= landmark.id %>"><%='checked' if @figure.landmarks.include?(landmark)%><br>
<% end %>

<h2> And/or add a new Landmark </h2>
<p><input type="text" id="new_landmark" name="figure[new_landmark]">New Landmark</p>


<h2>Choose the Figure's Titles(s)</h2>
<% Title.all.each do |title| %>
  <label><%= title.name %></label>
  <input type="checkbox" id="title_<%=title.id%>" name="figure[title_ids][]" value="<%= title.id %>"><%='checked' if @figure.titles.include?(title)%><br>
<% end %>

<h2> And/or add a new Title </h2>
<p><input type="text" id="new_title" name="figure[new_title]">New Title</p>



<input type="submit" value="Edit Figure">
</form>